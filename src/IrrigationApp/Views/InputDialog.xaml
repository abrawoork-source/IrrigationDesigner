<!-- File: src/IrrigationApp/Views/InputDialog.xaml -->
<Window x:Class="IrrigationApp.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="160" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Grid.Row="0" FontSize="13" Margin="0,0,0,8"
                   Foreground="#1E293B"/>
        <TextBox x:Name="InputBox" Grid.Row="1" FontSize="13"
                 Padding="8,6" Margin="0,0,0,12"
                 BorderBrush="#E2E8F0" BorderThickness="1"/>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="80" Margin="0,0,8,0"
                    IsDefault="True" Click="OkButton_Click"
                    Background="#1E40AF" Foreground="White"
                    BorderThickness="0" Padding="8,5"/>
            <Button Content="Cancel" Width="80"
                    IsCancel="True" Click="CancelButton_Click"
                    BorderBrush="#E2E8F0" Padding="8,5"/>
        </StackPanel>
    </Grid>
</Window>
